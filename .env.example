# Example environment configuration for Lime AI Scribe

### Backend (server) variables ###

# PostgreSQL connection string used by Prisma
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ai_scribe

# Port for the Express server
PORT=4000

# Base URL where your server is accessible.  Used to construct absolute URLs for local audio storage.
# For Docker Compose, this is typically http://localhost:${PORT}
PUBLIC_BASE_URL=http://localhost:4000

# OpenAI API key (optional).  When absent, the app falls back to stubbed AI functions.
#OPENAI_API_KEY=sk-...

# AWS credentials for S3 storage (optional).  When absent, audio files are stored on disk under `server/uploads`.
#AWS_ACCESS_KEY_ID=your-access-key
#AWS_SECRET_ACCESS_KEY=your-secret-key
#AWS_REGION=us-east-1

# S3 bucket name and optional URL expiry in seconds for pre‑signed download links.
#S3_BUCKET=your-bucket-name
#S3_URL_EXPIRES=3600

# Optional HTTP basic authentication credentials for the API.  When unset, no auth is required.
#AUTH_USER=admin
#AUTH_PASS=secret

# Comma‑separated list of origins allowed by CORS (optional).  Example: http://localhost:3000,https://yourdomain.com
#ALLOWED_ORIGINS=http://localhost:3000


### Frontend (client) variables ###

# Base URL of the backend API.  The Next.js frontend uses this to fetch data.
NEXT_PUBLIC_API_URL=http://localhost:4000